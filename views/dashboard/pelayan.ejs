<!DOCTYPE html>
<html>
<head>
  <title><%= title %></title>
  <style>
@import url('https://fonts.googleapis.com/css2?family=Quicksand:wght@300..700&display=swap');

    body {
      background-color: #F9FAFB;
      font-family: "Quicksand", sans-serif;
    }

.tab-content {
  display: none;
  animation: fadeIn 0.3s ease-in-out;
}

.tab-content.active {
  display: block;
}

.tabs button {
  margin-right: 10px;
  padding: 10px 15px;
  cursor: pointer;
  background: none;
  border: none;
  font-weight: 600;
  color: #555;
  border-bottom: 3px solid transparent;
  transition: all 0.2s ease;
}

.tabs button:hover {
  color: #333;
}

.tabs button.active {
  color: orange;
  border-bottom: 3px solid orange;
  background-color: transparent;
}

/* Statistik */
.stats-container {
  display: flex;
  flex-wrap: wrap;
  gap: 20px;
  margin-bottom: 20px;
}

.stat-box {
  flex: 1;
  text-align: center;
  padding: 15px;
  border: 1px solid #ddd;
  border-radius: 10px;
  min-width: 120px;
  background: #fff;
  box-shadow: 0 2px 6px rgba(0,0,0,0.05);
  transition: transform 0.2s;
}

.stat-box:hover {
  transform: translateY(-3px);
}

.pesanan-item {
  border: 1px solid #e0e0e0;
  padding: 20px;
  margin: 12px 0;
  border-radius: 12px;
  background: linear-gradient(135deg, #ffffff, #f9f9f9);
  box-shadow: 0 1px 4px rgba(0, 0, 0, 0.04);
  transition: all 0.25s ease-in-out;
  position: relative;
}

.pesanan-item:hover {
  box-shadow: 0 6px 18px rgba(0, 0, 0, 0.08);
  border-color: #ffa726;
  transform: translateY(-2px);
}

.pesanan-item::before {
  content: '';
  position: absolute;
  left: 0;
  top: 0;
  height: 100%;
  width: 4px;
  background: orange;
  border-radius: 12px 0 0 12px;
}


/* Modal */
.modal {
  display: none;
  position: fixed;
  z-index: 1000;
  left: 0;
  top: 0;
  width: 100%;
  height: 100%;
  overflow: auto;
  background-color: rgba(0,0,0,0.5);
  animation: fadeIn 0.3s ease-in-out;
}

/* Animasi Fade In */
@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

    .modal-content {
      background-color: #fefefe;
      margin: 2% auto;
      padding: 20px;
      border: 1px solid #888;
      width: 90%;
      max-width: 800px;
      border-radius: 8px;
      max-height: 90vh;
      overflow-y: auto;
    }
    .close {
      color: #aaa;
      float: right;
      font-size: 28px;
      font-weight: bold;
      cursor: pointer;
    }
    .close:hover { color: black; }
    
    /* Container pesanan */
.pesanan-item {
  position: relative;
  padding: 20px 20px 20px 16px;
  margin: 14px 0;
  border-radius: 12px;
  background: #ffffff;
  border: 1px solid #e6e8eb;
  box-shadow: 0 4px 14px rgba(0, 0, 0, 0.03);
  transition: transform 0.2s ease, box-shadow 0.2s ease;
  overflow: hidden;
}

.pesanan-item:hover {
  transform: translateY(-2px);
  box-shadow: 0 8px 26px rgba(0, 0, 0, 0.08);
}

/* Accent strip kiri */
.pesanan-item::before {
  content: "";
  position: absolute;
  left: 0;
  top: 0;
  width: 6px;
  height: 100%;
  background: linear-gradient(180deg, #ffb347, #ffcc33);
  border-radius: 12px 0 0 12px;
}

/* Header meja dan total */
.pesanan-item > h3 {
  margin: 0 0 6px;
  font-size: 1.1rem;
  font-weight: 700;
  color: #222;
}

.pesanan-item > div:first-of-type {
  margin-bottom: 10px;
  font-size: 0.95rem;
  color: #555;
  font-weight: 600;
}

/* Item menu di dalam pesanan */
.pesanan-item .menu-entry {
  border: 1px solid #d9dce0;
  padding: 12px 14px;
  margin: 8px 0;
  border-radius: 10px;
  background: #f9f9fb;
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
  align-items: center;
  position: relative;
  font-size: 0.9rem;
}

.pesanan-item .menu-entry strong {
  display: block;
  font-size: 1rem;
  margin-bottom: 4px;
  color: #1f2d3a;
}

/* Aksi tombol */
.menu-actions {
  margin-left: auto;
  display: flex;
  gap: 8px;
  flex-shrink: 0;
}

.menu-actions button {
  padding: 6px 12px;
  border: none;
  border-radius: 8px;
  font-size: 0.8rem;
  cursor: pointer;
  font-weight: 600;
  transition: filter 0.15s ease;
}

.btn-delete {
  background: #ffe5e5;
  color: #c0392b;
}

.btn-delete:hover {
  filter: brightness(1.05);
}

.btn-change {
  background: #e8f4ff;
  color: #1565c0;
}

.btn-change:hover {
  filter: brightness(1.05);
}

/* Sub total */
.menu-entry div {
  flex: 1 1 150px;
  min-width: 120px;
  color: #444;
}

/* Responsif kecil */
@media (max-width: 800px) {
  .pesanan-item {
    padding: 16px 16px 16px 12px;
  }

  .menu-actions {
    width: 100%;
    justify-content: flex-start;
    gap: 6px;
    margin-top: 6px;
  }

  .menu-entry {
    flex-direction: column;
    align-items: flex-start;
  }
}


    .order-item {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 8px;
      border: 1px solid #eee;
      margin: 5px 0;
      border-radius: 4px;
    }
    .quantity-controls {
      display: flex;
      align-items: center;
      gap: 10px;
    }
    .quantity-controls button {
      width: 30px;
      height: 30px;
      border: 1px solid #ccc;
      background: white;
      cursor: pointer;
      border-radius: 3px;
    }
    .form-tab { display: none; }
    .form-tab.active { display: block; }
    .menu-actions {
      display: flex;
      gap: 10px;
      margin-top: 10px;
    }
    .menu-actions button {
      padding: 5px 10px;
      border: none;
      border-radius: 4px;
      cursor: pointer;
    }

    .stat-box {
      width: 200px;
      height: 80px;
      display: flex;
      flex-direction: column;
      justify-content: center;
      background-color: white;
      margin: 20px 0;
    }

    .tabs button {
      margin-right: 0;
      border: 0;
      background-color: white;
    }
  </style>
</head>
<body>
  <header style="background-color: white; max-width: 1200px; margin: auto;">
  <div style="display: flex; justify-content: space-between; align-items: center;">
    <h1><%= title %></h1>
    <form action="pelayan/logout" method="post">
      <button type="submit" style="padding: 10px 20px; background-color: #f44336; color: white; border: none; border-radius: 5px; cursor: pointer;">Logout</button>
    </form>
  </div>
  </header>

  <div class="stats-container" style="display: flex; justify-content: space-between; max-width: 1200px; margin: auto;">
    <div class="stat-box">
      <strong>Meja Tersedia</strong><br/>
      <span style="font-size: 24px;"><%= meja.filter(p => p.status_meja === 'KOSONG').length %></span>
    </div>
    <div class="stat-box">
      <strong>Meja Terisi</strong><br/>
      <span style="font-size: 24px;"><%= meja.filter(p => p.status_meja === 'TERISI').length %></span>
    </div>
    <div class="stat-box">
      <strong>Pesanan Pending</strong><br/>
      <span style="font-size: 24px;"><%= pesanan.filter(p => p.status_pesanan === 'BELUM_DIMASAK').filter(p => menuPesanan.filter(m => m.id_pesanan === p.id_pesanan).some(m => m.ketersediaan === 'TIDAK_TERSEDIA')).length %></span>
    </div>
    <div class="stat-box">
      <strong>Siap Disajikan</strong><br/>
      <span style="font-size: 24px;"><%= pesanan.filter(p => p.status_pesanan === 'SIAP_DISAJIKAN').length %></span>
    </div>
  </div>

  <!-- Tab Belum Dimasak -->
  <main style="max-width: 1200px; margin: auto; background-color: white; padding-bottom: 50px; box-sizing: border-box;">
    <div class="tabs">
      <button onclick="showTab('tab-belum-dimasak')" id="btn-belum-dimasak" class="btn-tab">Belum Dimasak</button>
      <button onclick="showTab('tab-sedang-dimasak')" id="btn-sedang-dimasak" class="btn-tab">Sedang Dimasak</button>
      <button onclick="showTab('tab-menu-tidak-tersedia')" id="btn-menu-tidak-tersedia" class="btn-tab">Menu Tidak Tersedia</button>
      <button onclick="showTab('tab-siap-disajikan')" id="btn-siap-disajikan" class="btn-tab">Siap Disajikan</button>
      </div>
    <div id="tab-belum-dimasak" class="tab-content" style="padding: 20px;">
    <h2>Belum Dimasak</h2>
    <% pesanan.filter(p => p.status_pesanan === 'BELUM_DIMASAK').forEach(p => { %>
      <div class="pesanan-item">
        <strong>Meja <%= p.id_meja %></strong><br/>
        <div style="margin: 10px 0;">
          <%= menuPesanan.filter(m => m.id_pesanan === p.id_pesanan && m.ketersediaan !== 'TIDAK_TERSEDIA').map(m => `${m.jumlah}x ${m.nama_menu}`).join(', ') %>
        </div>
        <div>Total: Rp <%= p.total_harga || 0 %></div>
      </div>
    <% }) %>
  </div>

  <!-- Tab Sedang Dimasak -->
  <div id="tab-sedang-dimasak" class="tab-content" style="padding: 20px;">
    <h2>Sedang Dimasak</h2>
    <% pesanan.filter(p => p.status_pesanan === 'SEDANG_DIMASAK').forEach(p => { %>
      <div class="pesanan-item">
        <strong>Meja <%= p.id_meja %></strong><br/>
        <div style="margin: 10px 0;">
          <%= menuPesanan.filter(m => m.id_pesanan === p.id_pesanan).map(m => `${m.jumlah}x ${m.nama_menu}`).join(', ') %>
        </div>
        <div>Total: Rp <%= p.total_harga || 0 %></div>
      </div>
    <% }) %>
  </div>

  <!-- Tab Menu Tidak Tersedia -->
  <div id="tab-menu-tidak-tersedia" class="tab-content" style="padding: 0 20px;">
    <h2>Menu Tidak Tersedia</h2>
    <% const menuTidakTersedia = menuPesanan.filter(m => m.ketersediaan === 'TIDAK_TERSEDIA');
      const grouped = {};

      menuTidakTersedia.forEach(m => {
          if (!grouped[m.id_pesanan]) grouped[m.id_pesanan] = [];
          grouped[m.id_pesanan].push(m);
      });

      for (const id_pesanan in grouped) {
        const pesananData = pesanan.find(p => p.id_pesanan == id_pesanan);
        if (!pesananData) continue; %>
        <div class="pesanan-item">
          <h3>Meja <%= pesananData.id_meja %></h3>
          <div>Total Pesanan: Rp <%= pesananData.total_harga || 0 %></div>
          <% grouped[id_pesanan].forEach(m => { %>
            <div style="border: 1px solid #ccc; padding: 10px; margin: 5px 0; background: #fff;">
              <div><strong><%= m.jumlah %>x <%= m.nama_menu %></strong></div>
              <div>Sub Total: Rp <%= m.sub_total %></div>
              <div class="menu-actions">
                <button class="btn-delete" onclick="deleteMenuPesanan('<%= m.id_menu %>', '<%= m.id_pesanan %>')">Hapus</button>
                <button class="btn-change" onclick="openChangeMenuModal('<%= m.id_menu %>', '<%= m.id_pesanan %>', '<%= m.nama_menu %>', '<%= m.jumlah %>')">Ganti</button>
              </div>
            </div>
          <% }) %>
        </div>
    <% } %>
  </div>

  <!-- Tab Siap Disajikan -->
  <div id="tab-siap-disajikan" class="tab-content" style="padding: 0 20px;">
    <h2>Siap Disajikan</h2>
    <% pesanan.filter(p => p.status_pesanan === 'SIAP_DISAJIKAN').forEach(p => { %>
      <div class="pesanan-item">
        <strong>Meja <%= p.id_meja %></strong><br/>
        <div style="margin: 10px 0;">
          <%= menuPesanan.filter(m => m.id_pesanan === p.id_pesanan).map(m => `${m.jumlah}x ${m.nama_menu}`).join(', ') %>
        </div>
        <div>Total: Rp <%= p.total_harga || 0 %></div>
        <button class="btn-served" onclick="updateStatusPesanan('<%= p.id_pesanan %>', '<%= id_pelayan %>', 'SUDAH_DISAJIKAN')">Sudah Disajikan</button>
      </div>
    <% }) %>
  </div>

  <!-- Modal Ganti Menu -->
  <div class="modal" id="modal-ganti-menu">
    <div class="modal-content">
      <span class="close" onclick="closeChangeMenuModal()">&times;</span>
      <h2>Ganti Menu</h2>

      <!-- Form Tab Pilih Menu -->
      <div class="form-tab active" id="tab-pilih-menu">
        <h3>Pilih Menu Pengganti</h3>
        <div id="menu-list">
          <% menu.forEach(({ id_menu, nama_menu, harga, jenis_menu }) => { %>
            <div class="menu-item">
              <div><strong><%= nama_menu %></strong></div>
              <div>Harga: Rp <%= harga.toLocaleString('id-ID') %></div>
              <div>Jenis: <%= jenis_menu %></div>
              <button type="button" onclick="selectNewMenu('<%= id_menu %>', '<%= nama_menu %>', '<%= harga %>')">Pilih</button>
            </div>
          <% }) %>
        </div>
      </div>

      <!-- Form Tab Konfirmasi Penggantian -->
      <div class="form-tab" id="tab-konfirmasi">
        <h3>Konfirmasi Penggantian</h3>
        <div id="change-summary"></div>
        <div class="quantity-controls">
          <label>Jumlah:</label>
          <button type="button" onclick="changeQuantity(-1)">-</button>
          <span id="new-quantity">1</span>
          <button type="button" onclick="changeQuantity(1)">+</button>
        </div>
        <div style="margin: 20px 0;">
          <strong>Sub Total: Rp <span id="new-subtotal">0</span></strong>
        </div>
        <div style="display: flex; gap: 10px;">
          <button type="button" onclick="showFormTab('tab-pilih-menu')">Kembali</button>
          <button type="button" onclick="confirmChangeMenu()">Konfirmasi Perubahan</button>
        </div>
      </div>
    </div>
  </div>
  </main>

  <script src="/javascripts/pelayan.js"></script>
</body>
</html>
